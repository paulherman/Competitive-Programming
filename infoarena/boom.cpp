#include <iostream>
using namespace std;


int main() {
    freopen("boom.in", "r", stdin);
    freopen("boom.out", "w", stdout);

    int rooms, rounds;
    cin >> rooms >> rounds;
    
    return 0;
}
